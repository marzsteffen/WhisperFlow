[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "local-dictation"
version = "0.1.0"
description = "Fully local push-to-talk dictation for Windows and Linux"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "local-dictation contributors" }]
dependencies = [
  "PyQt6>=6.7",
  "requests>=2.31",
  "evdev-binary>=2.0; platform_system == 'Linux'",
  "pyudev>=0.24; platform_system == 'Linux'",
  "keyboard>=0.13.5; platform_system == 'Windows'",
  "sounddevice>=0.5.1; platform_system == 'Windows'",
  "pyperclip>=1.9; platform_system == 'Windows'",
]

[project.scripts]
local-dictation = "local_dictation.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
local_dictation = ["resources/*.wav", "resources/*.json"]

[tool.pytest.ini_options]
addopts = "-ra"
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
  "integration: requires installed whisper-server and local models",
  "live: interacts with the active Wayland desktop and real input devices",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B"]
ignore = [
  "E402",  # Standalone unittest modules add src/ before local imports.
  "UP042", # str+Enum preserves the public JSON/string API on Python 3.12.
]
